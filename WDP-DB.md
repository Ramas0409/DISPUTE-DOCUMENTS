# WDP-DB.md
**Worldpay Dispute Platform — Database Schema Ownership Map**
*Version: 2.2*

---

## Purpose of This Document

This is the master database reference for the WDP platform. It contains:

1. **Database Instances** — all database systems WDP connects to and
   who owns them
2. **Schema Ownership Map** — every schema and table, which component
   owns it (writes to it), and which components access it read-only
3. **Shared Table Risk Register** — tables written by more than one
   component, flagged as coordination risks
4. **Data Retention and Compliance Notes**

Individual component files (WDP-COMP-[NN]-*.md) contain the Database
Ownership section specific to each component. This file is the
cross-cutting view — the complete data ownership map for the platform.

> ⚠️ **Schema-DDL caveat (platform-wide):** No component repository
> contains Flyway / Liquibase / `schema.sql` / `data.sql`. All `ddl-auto`
> settings are `false` and all `spring.batch.jdbc.initialize-schema`
> settings are `never`. DDL — including UNIQUE constraints, indexes, and
> column existence on shared tables — is managed in a schema-owning
> repository outside the component repos. **Unique-constraint
> enforcement on every shared table below cannot be confirmed from the
> component sources alone.** DBA confirmation is required for every
> uniqueness assumption in this document, including:
> `(idempotency_id, channel_type, event_timestamp)` on `wdp.outgoing_event_outbox`,
> `(file_name, s3_key)` and `(chbk_outbox_row_id)` on file/evidence tables,
> `(file_job_id, event_type, row_number)` and `(c_ntwk_case_id, c_ntwk_phase_id)` on `wdp.chbk_outbox_row`,
> `(I_CASE, I_ACTION_SEQ)` on `wdp.disputes_questionnaire`,
> `(c_consumer_name, c_source_system, c_entity_type, c_entity_value)` on `wdp.acl`,
> `c_name` on `nap.nap_parent_entity`,
> `(i_parent_entity_id, c_name)` on `nap.nap_child_entity`,
> `(i_parent_entity_id, i_child_entity_id, i_mid)` on `nap.nap_entity_rel`,
> `hpan` on `wdp.hash_key_store`,
> and the existence of any unique key on `NAP.DISPUTE_EVENT_CONSUMER_ERROR` and `NAP.BUSINESS_RULE_CONSUMER_ERROR`.

> ⚠️ **Cluster topology caveat:** JDBC URLs for
> `wdpdataSource` and `napdataSource` are externalised via K8s secrets in every component.
> Whether the `wdp` and `nap` schemas resolve to the same physical Aurora cluster, or two
> distinct clusters, is **not determinable from any component source**. DBA / infrastructure
> confirmation is required for capacity-planning and blast-radius reasoning.

---

## 1. Database Instances

| Database | Type | Owner | Purpose | WDP Components That Connect |
|----------|------|-------|---------|----------------------------|
| WDP Aurora PostgreSQL (globaldisputedatabase / wpdisputedatabase) | Amazon Aurora PostgreSQL | WDP Team | Primary WDP operational database. Contains all dispute case data, outbox tables, file processing tables, rules, notes, questionnaires, org/user data, queues, ACLs, encryption key tables, and configuration. | Most WDP components (wdp schema). New writers: COMP-30 UserQueueSkillService (queues / queue_criterion / user_queue), COMP-35 EncryptionService (hash_key_store / data_enc_key). |
| NAP Aurora PostgreSQL | Amazon Aurora PostgreSQL | WDP Team | NAP acquiring platform data — entity hierarchy, MIDs, NAP case data, batch metadata, NAP rules, NAP error/DLQ tables. | COMP-02 UAMS, COMP-05 NAPDisputeEventProcessor, COMP-06 NAPDisputeDeclineBatch, COMP-23 CaseManagementService, COMP-24 CaseActionService, COMP-25 NotesService, COMP-30 UserQueueSkillService, COMP-39 NAPOutcomeProcessor, COMP-16 BusinessRulesProcessor (read-only), COMP-31 BusinessRulesService, COMP-32 RulesService (read-only). **COMP-04 NAPDisputeEventService confirmed stateless** — no DB connection (source-verified). |
| IBM DB2 — Core Platform (BC, MD, DC schemas) | IBM DB2 | Enterprise (not WDP owned) | Core enterprise merchant hierarchy and Core platform dispute records. Read-only for most WDP components. COMP-43 CoreNotificationConsumer is the sole WDP writer to the BC schema dispute tables. | COMP-03 CHAS (read-only MD/DC/BC), COMP-15 EvidenceConsumer (read-only BC — V3 path), COMP-22 DisputeService (read-only BC — when coreMigrationStatus=false; source-verified zero writes), COMP-34 MerchantTransactionService (read-only BC — CapOne enrichment), COMP-43 CoreNotificationConsumer (READ+WRITE BC.TBC_DM_CASE; READ-ONLY for enrichment is REST-driven, NOT direct DB2 reads — see Section 3) |
| MS SQL Server — Legacy Fax System | Microsoft SQL Server | External (legacy — not WDP owned) | Legacy fax queue storage shared with legacy fax processing systems outside WDP | COMP-29 FaxQueueService (read + status update — claim, reject, discard operations on dbo.IncomingFaxes) |
| Amazon DynamoDB | DynamoDB | WDP Team | Evidence document metadata. Used exclusively by DocumentManagementService. Two tables (`WDP_PIN_DISPUTE_DOCUMENTS`, `NAP_DISPUTE_DOCUMENTS`) declare 5 GSIs each (`C_STAGE_CODE`, `I_ACTION_SEQ`, `C_DOC_TYPE`, `N_DOC_NAME`, `Z_UPDT`) — none queried from Java. GSI projection type not determinable from source — confirm via AWS console / Terraform / CloudFormation. | COMP-37 DocumentManagementService (owns) |
| AWS ElastiCache | Redis | WDP Team (writer unknown) | JWT token cache. Redis hash key: `wdpinternalidptoken`, field: `token`. Value: Jackson-serialised `IdpToken` (tokenValue, expiresIn, tokenType, expirationDate). **COMP-36 TokenService reads this hash (HGET only — zero write operations exist anywhere in the wdp-idp-token-service repository, confirmed).** Hash is populated by an UNKNOWN EXTERNAL COMPONENT not present in any audited WDP repo. ⚠️ External writer identity is an open question — see COMP-36 file. | COMP-36 TokenService (reads only — does NOT own writes) |

---

## 2. Schema Ownership Map — WDP Aurora PostgreSQL

### Schema: `wdp`

*The primary WDP schema. Owned by the WDP Core team.*

| Table | Owning Component(s) | Purpose | Key Columns | Accessed Read-Only By | Confirmed |
|-------|---------------------|---------|-------------|----------------------|-----------|
| `wdp.chbk_outbox_row` | COMP-07 VisaDisputeBatch (INSERT — PENDING / SKIPPED on duplicate / ERROR on missing key data — `skipCase` items write no row at all); COMP-08 FirstChargebackBatch (INSERT — PENDING / SKIPPED marker rows on duplicate); COMP-09 CaseFillingBatch (INSERT-only — never UPDATE); COMP-11 FileProcessor (INSERT — PENDING rows; transiently LOADING during evidence phase; BLOCKED for DCPO evidence rows); COMP-12 InboundDisputeEventScheduler (status transitions + Kafka metadata write-back on `new-case-events` and `case-evidence-events` paths only); COMP-14 CaseCreationConsumer (status transitions ONLY — `FAILED`, `ERROR`, `SUCCESS`, `PENDING_DEFERRED`, `SKIPPED`, and `PENDING` on EVIDENCE_ATTACH children); COMP-15 EvidenceConsumer (status updates — `FAILED`, `ERROR`, `SUCCESS`); COMP-23 CaseManagementService (conditional — `findById(...).isPresent()` guard, **no terminal-status guard**, can overwrite a PROCESSED row; same `@Transactional` as case save); COMP-24 CaseActionService (status UPDATE only — US path) | Shared transactional outbox. Dispute events (CHARGEBACK_PROCESS) and evidence (EVIDENCE_ATTACH) queued for Kafka publishing via COMP-12. Key cols: id, file_job_id, row_number, parent_row_number, event_type, status (LOADING/PENDING/PUBLISHED/SUCCESS/ERROR/BLOCKED/SKIPPED/PENDING_DEFERRED), payload (JSON), c_ntwk_case_id, c_ntwk_phase_id (= caseFilingDetails.caseId for COMP-09), c_case_stage, c_case_ntwk, c_acq_platform, c_level1_entity, kafka_topic, kafka_partition, kafka_offset, idempotency_id (typed UUID — cross-table type inconsistency with `outgoing_event_outbox` String), retry_count, next_retry_at, published_at, source_event, document_type, record_detail (JSONB), created_by, updated_by | COMP-13 FileAcknowledgementProcessor (per-row ACK detail — read-only). | ✅ Confirmed from COMP-07, COMP-08, COMP-09, COMP-11, COMP-12, COMP-13, COMP-14, COMP-15, COMP-23, COMP-24 audits |

**`wdp.chbk_outbox_row` — writer-specific notes:**

- **`created_by` / `updated_by` constants — four distinct values across batch writers:**
  - COMP-07 VisaDisputeBatch: `"WVDPB"`
  - COMP-08 FirstChargebackBatch: `"WMFDPB"`
  - COMP-09 CaseFillingBatch: `"WMPAPB"`
  - COMP-11 FileProcessor: `"FILE_PROCESSOR"`
- **No DB unique constraint confirmed on this table** — no DDL / Flyway / Liquibase / entity-level `uniqueConstraints` in COMP-07, COMP-08, COMP-11, COMP-13, or COMP-23 repos. `ddl-auto: false`; `initialize-schema: never`. Whether a unique index exists in the live schema must be confirmed with the DBA team.
- **COMP-09 update path always INSERTs a new row** — existing rows are never UPDATEd by COMP-09. A claim that progresses through its phase lifecycle produces multiple rows, one per phase observation, distinguished by status (PENDING / SKIPPED). Table growth is proportional to phase count × rerun frequency.
- **COMP-09 writer swallows all save exceptions** — DB save failure produces no error row, no retry, and no Spring Batch skip counter increment; the chunk reports success. Same pattern observed on COMP-07's `BatchItemWriter`. Cross-batch platform pattern.
- **COMP-09 skip paths write no row at all** — claims that fail null-field validation, stage-determination, IDP token, or encryption leave zero database trace. `error_code` / `error_message` columns exist on the entity but are never populated by COMP-09.
- **COMP-09 c_case_stage concatenation** — base stage (`PAB`, `ARB`, `PRA`, `AII`, `AIM`) is written EXCEPT when `issuerCaseStatus = withdraw`, where the column is written as `{stage}_withdraw` (e.g. `PAB_withdraw`, `PRA_withdraw`). Downstream consumers that key on `c_case_stage` must tolerate this concatenation.
- **COMP-09 `network_notes` column** — written conditionally, only when `schemeRef.notes` is non-empty. No other batch writer populates this column.
- **COMP-09 `c_ntwk_case_id` semantics** — value is `standardClaimId`, which equals `claimDetail.standardClaims` when `claimType = "CaseFiling"`, else `claimId`. The DRAFT label `networkCaseId` is loose.
- **COMP-08 duplicate-check writes SKIPPED marker rows rather than suppressing the insert** — `skipCase` flag on `ProcessedItem` is computed but never consumed by its Writer. One SKIPPED row accumulates per re-polled known chargeback per COMP-08 scheduler run. Growth boundedness depends on whether COMP-12 Scheduler2 archives SKIPPED rows — not currently confirmed.
- **COMP-08 update-path writes status=PENDING with accountNumber=null** on newly-arrived chargebacks on an existing claim — `processUpdatedClaims` never sets `isAccountNumberRequired=true`. Suspected defect.
- **COMP-08 writer-ACK hazard** — mid-chunk JPA save failure does not prevent the subsequent ACK PUT to MCM; ACK PUT exceptions are swallowed silently with no log, no metric, no outbox marker. Items acknowledged off MCM with no corresponding outbox row are unrecoverable.
- **COMP-07 `skipCase` items write no outbox row at all** (previously implied SKIPPED was written — corrected).
- **COMP-11 entity in this repo does not declare `kafka_topic` / `kafka_partition` / `kafka_offset` / `idempotency_id` / `retry_count` / `next_retry_at` / `published_at`** — consistent with COMP-12 being sole setter of these columns.
- **COMP-11 initial CHARGEBACK_PROCESS status is PENDING (PrePersist).** LOADING is transient during the evidence phase only (parent flipped by `insertEvidenceOutBox @Transactional`). DCPO evidence rows are explicitly BLOCKED; DCPO parent stays PENDING. DNWK writes CHARGEBACK_PROCESS at PENDING directly with no evidence phase.
- **COMP-12 idempotencyId is typed as `UUID` on this table's entity**, in contrast to `wdp.outgoing_event_outbox` (String) — cross-table type inconsistency to flag for any downstream consumer doing shared dedup logic.
- **COMP-23 `findById(...).isPresent()` UPDATE guard is no-op if the row is missing.** No terminal-status guard — a PROCESSED row can be overwritten. Same `@Transactional` as the case save.
- **SUCCESS rows are archived to `wdp.chbk_outbox_row_archive` by COMP-12 Scheduler2 after 30 days.**

---

| Table | Owning Component(s) | Purpose | Key Columns | Accessed Read-Only By | Confirmed |
|-------|---------------------|---------|-------------|----------------------|-----------|
| `wdp.chbk_outbox_row_archive` | COMP-12 InboundDisputeEventScheduler (Scheduler2) | Long-term archive of SUCCESS rows moved from chbk_outbox_row. 30-day threshold hardcoded. Mirrors chbk_outbox_row columns + archived_at. ⚠️ Archive INSERT SQL references column `c_ntwrk_phase_id` that the `ChbkOutboxArchiveEntity` does not map — either dead column reference or incomplete entity. **DDL verification required** (schema lives outside this repo). No purge policy confirmed for archive table itself. | Mirrors chbk_outbox_row + archived_at | None confirmed | ✅ Confirmed from COMP-12 analysis |
| `wdp.file_job` | COMP-11 FileProcessor (INSERT + UPDATE — owns columns: `id`, `file_name`, `s3_key`, `s3_bucket`, `file_size_bytes`, `source`, `status`, `total_rows`, `total_evidences`, `ack_required`, `ack_status`, `error_code`, `error_message`, `created_by="FILE_PROCESSOR"`, `updated_by="FILE_PROCESSOR"`); COMP-12 InboundDisputeEventScheduler (Scheduler2 — `status` → COMPLETED, `completed_at`); COMP-13 FileAcknowledgementProcessor (writes EXACTLY four columns enforced by `@DynamicUpdate`: `ack_status`, `ack_generated_at`, `updated_at`, `updated_by`) | File-level processing ledger. One row per inbound ZIP. Status lifecycle: PENDING / PROCESSING / ERROR / COMPLETED. ⚠️ **COMP-11 never writes COMPLETED** (only PROCESSING) — successful runs terminate at PROCESSING. COMP-12 Scheduler2 owns the COMPLETED transition. COMP-13 polls for `status IN (COMPLETED, ERROR)` so the ACK pipeline depends entirely on COMP-12 actually flipping the status. ⚠️ **Cross-component COMPLETED-transition contract is undocumented in all three repos** — open question OQ-4 / OQ-FileJob-COMPLETED-Owner. ⚠️ Columns that may exist in the live schema but are NOT bound on the COMP-11 entity (and which COMP-11 does not write): `successful_rows`, `failed_rows`, `error_rows`, `attached_evidences`, `failed_evidences`. Whether these are populated by other components or by triggers is not determinable from current sources. | None other than the listed writers | ✅ Confirmed from COMP-11, COMP-12, COMP-13 analysis |
| `wdp.file_evidence` | COMP-11 FileProcessor (INSERT — inside `insertEvidenceOutBox @Transactional`; created_by="FILE_PROCESSOR" — corrected from "WPFLEPR"); COMP-15 EvidenceConsumer (UPDATE — `attachment_status`, `appended_file_name`, `failed_s3_key`, `attached_at`) | Evidence document index. One row per extracted document. Key cols: id, file_job_id (FK), chbk_outbox_row_id (FK), file_name, s3_key (staging path), s3_bucket, attachment_status (PENDING/ATTACHED/FAILED), attached_at, attachment_error, failed_s3_key, i_case, c_ntwk_case_id, created_by="FILE_PROCESSOR". Not written for DNWK sources. ⚠️ **COMP-15 V3 path defect** — MISCDOC / DRFTDOC / RESPQDOC / ISSRQDOC document types silently mark `attachment_status=ATTACHED` without any upload occurring. `appended_file_name` may be `null` on V3-path ATTACHED rows; downstream readers should not assume it is populated. ⚠️ No DB-level UNIQUE constraint on `(chbk_outbox_row_id)` confirmed in repo — rolling deploy replica overlap can produce duplicate upload. | COMP-12 InboundDisputeEventScheduler (Scheduler5 — error report only, read-only). COMP-13 does not access this table. COMP-15 reads `s3_bucket`, `s3_key`, `file_name`, `attachment_status` for document retrieval. | ✅ Confirmed from COMP-11, COMP-12, COMP-15 analysis |
| `wdp.CASE` | COMP-23 CaseManagementService (primary — all case create/update operations via `wdpTransactionManager`); COMP-24 CaseActionService (case open/close transitions, same transaction as action write); COMP-15 EvidenceConsumer (conditional — Z_UPDT timestamp update on RESPDOC WDP path only); COMP-27 CaseSearchService (column-level UPDATE on V1 PATCH and V2 POST case-lock endpoints — writes only `c_case_lock`, `c_locked_by`, `z_locked_at`, `x_updt`, `z_updt`. No `@Transactional`, no `SELECT FOR UPDATE`, no optimistic version. Auto-commit JDBC); COMP-37 DocumentManagementService (column-level UPDATE on desk-blanking branch — INSERT/DELETE owner remains UNCONFIRMED; **COMP-22 confirmed source-verified zero writes**, so the prior speculation that COMP-22 might be the INSERT/DELETE owner is withdrawn) | Central dispute case record for all US-platform disputes (PIN, CORE, VAP, LATAM). Key cols: I_CASE (PK / caseNumber), C_CASE_STA (OPEN/CLOSED), C_CASE_FINAL_LIABILITY, I_CASE_ACTION_MAX_SEQ, Z_UPDT, C_LEVEL1_ENTITY through C_LEVEL5_ENTITY, C_CASE_NTWK, C_CASE_LOCK, C_LOCKED_BY, Z_LOCKED_AT, **I_ACCT_CDH** (⚠️ clear PAN written by COMP-23 on standard create — DEC-004 violation; same column name across both schemas). ⚠️ **NAP/WDP lock-timestamp column-name drift** — NAP uses `x_locked_at`, WDP uses `z_locked_at`. | COMP-03 CHAS (read-only — `findByCaseNumber()`, columns `I_CASE`, `C_LEVEL1_ENTITY`, `C_LEVEL4_ENTITY`), COMP-14 CaseCreationConsumer (read-only — Capone REQ duplicate check), COMP-16 BusinessRulesProcessor (read-only — rule evaluation), COMP-22 DisputeService (read-only — dispute summary; source-verified), COMP-27 CaseSearchService (read-only for search/detail/lookup/dispute summary), COMP-30 UserQueueSkillService (read-only — confirmed source-verified), COMP-43 CoreNotificationConsumer (read-only — WDP enrichment before DB2 write — REST-driven, no DB2 enrichment reads) | ✅ Confirmed from COMP-15, COMP-22, COMP-23, COMP-24, COMP-27, COMP-30, COMP-37 audits |
| `wdp.ACTION` | COMP-23 CaseManagementService (inserts new actions on case create/update, cascaded via USCaseEntity one-to-many, same transaction as wdp.CASE write); COMP-24 CaseActionService (inserts and updates action status, owner, liability — no `@Version`, no pessimistic lock, no advisory lock); COMP-15 EvidenceConsumer (conditional — C_OWNR transfer from MERCHANT to WPAYOPS on RESPDOC WDP path only) | WDP case action records. Key cols: I_ACTION_SEQ, C_SOURCE_CASE_ID, C_SOURCE_UNIQUE_ID, C_ACTION_TYPE, C_ACTION_STA (OPEN/CLOSED), C_ACTION_STAGE, C_OWNER, C_CASE_FINAL_LIABILITY, D_EXPIRY_DUE, D_RESPONSE_DUE. ⚠️ No `@UniqueConstraint`, no `@Version`, no `SELECT FOR UPDATE` on any WDP entity. COMP-24 open-action constraint enforcement is in-memory only — race window on concurrent POSTs for same caseNumber. | COMP-16 BusinessRulesProcessor (read-only — rule evaluation), COMP-22 DisputeService (read-only — dispute summary; source-verified), COMP-24 CaseActionService (read for update), COMP-27 CaseSearchService (read-only for search/detail/summary/queue filters), COMP-30 UserQueueSkillService (read-only — confirmed source-verified), COMP-43 CoreNotificationConsumer (read-only — REST-driven enrichment) | ✅ Confirmed from COMP-15, COMP-22, COMP-23, COMP-24, COMP-27, COMP-30 audits |
| `wdp.NOTES` | COMP-25 NotesService (primary — all non-NAP platform notes via `wdpTransactionManager`); COMP-23 CaseManagementService (US case create path — ⚠️ **duplicate-insert source defect**: two identical `USNotesEntity` save blocks execute when `notesRequest != null`, producing two rows per create. Defect not fixed in current working tree.); COMP-24 CaseActionService (conditional — when note field present in request, same transaction as action write) | Dispute case notes for PIN/CORE/VAP/LATAM platforms. Immutable audit trail. Key cols: I_NOTE_ID (PK, seq), I_CASE, I_ACTION_SEQ, C_NOTE_TYPE, T_NOTE, D_NOTE, X_INSRT (userId), Z_INSRT, X_INSRT_DISPLAY, X_UPDT_DISPLAY. ⚠️ **COMP-25 has no awareness of COMP-23 / COMP-24 co-writers** — no `@UniqueConstraint`, no `@Version`, no advisory lock, no co-ordination annotation. | ⚠️ Downstream readers TBC | ✅ Confirmed from COMP-23, COMP-24, COMP-25 audits |
| `wdp.disputes_questionnaire` | COMP-26 QuestionnaireService (primary — all questionnaire creates and updates; **NO `@Transactional` boundary on any path** — every JPA call runs in Spring Data JPA's per-call default transaction. PUT (find → save) and B1 UPSERT (find → decide → save) span multiple short transactions and are vulnerable to TOCTOU races. App tier uses `DriverManagerDataSource` — no HikariCP; effective pooling depends on PgBouncer / Aurora-side); COMP-37 DocumentManagementService (UPSERT on Endpoint 11 questionnaire path, `@Transactional(rollbackOn=Exception.class)` — last-write-wins on `(caseNumber, actionSequence)` PK; republishes Kafka each time; no idempotency guard beyond PK); COMP-15 EvidenceConsumer (upserts document names on RESPDOC WDP path — confirmed scope) | Dispute-response questionnaire store. One row per (caseNumber, actionSeq). Full questionnaire serialised as JSON blob in C_QSTNNAIR. Key cols: I_QSTNNAIR_ID (PK, auto-increment seq), I_CASE, I_ACTION_SEQ, C_CASE_STAGE, C_QSTNNAIR (JSON blob), N_DOCUMENT_NAME, userId, createdAt, updatedAt. ⚠️ SHARED TABLE — no confirmed DB unique constraint on (I_CASE, I_ACTION_SEQ); COMP-26 duplicate POST inserts new row rather than updating, AND COMP-26 B1 UPSERT path is exposed to concurrent insert races. | ⚠️ Downstream readers TBC | ✅ Confirmed from COMP-15, COMP-26, COMP-37 audits |
| `wdp.disputes_questionnaire_doc_status_rules` | ⚠️ Owner TBC — no INSERT/UPDATE/DELETE in COMP-26 repo; no migration files. Writer must be located by cross-repo grep. | Reference / lookup configuration controlling questionnaire document-status routing. Read by COMP-26 QuestionnaireService at runtime. | ⚠️ TBC — column list not yet captured | COMP-26 QuestionnaireService (read-only) | ⚠️ PENDING — confirm write owner |
| `wdp.case_expiry` | COMP-17 CaseExpiryUpdateConsumer (INSERT/UPSERT/DELETE on action close — application-level UPSERT with no DB-level INSERT…ON CONFLICT or MERGE; deleted on CLOSED. UPDATE branch overwrites only `d_response_due`, `d_expiry_due`, `z_updt`. Each `case_expiry` write is its own `@Transactional REQUIRED` transaction, **independent of outbox writes** — non-atomic split risk); **COMP-51 CaseExpiryProcessor** | Active expiry schedule per case+action. Key cols: i_case (caseNumber), i_action_seq, c_acq_platform, d_expiry_due, d_response_due, i_retry_count, c_workflow_name, **z_insrt**, z_updt. ⚠️ **Shared write boundary between COMP-17 and COMP-51 with no coordination** — no row-level lock, no version column, no SELECT FOR UPDATE. Race between COMP-17 upsert and COMP-51 retry-counter UPDATE possible. Documented and pending architect decision. | **COMP-51 CaseExpiryProcessor** confirmed as the primary downstream reader (resolved partially — other downstream readers, if any, still need cross-component sweep on remaining WDP repos). | ✅ Confirmed from COMP-17, COMP-51 source |
| `wdp.outgoing_event_outbox` | COMP-17 CaseExpiryUpdateConsumer (`channel_type=EXPIRY_EVENTS`, created_by=`WCSEEXPC`); COMP-41 ThirdPartyNotificationConsumer (`channel_type=GP_EVENTS`; created_by=`WNEC`); COMP-42 BENConsumer (`channel_type=BEN_EVENTS`, created_by/updated_by=`WBENC`; status set used: PUBLISHED / SUCCESS / FAILED / ERROR / PENDING_DEFERRED — SKIPPED defined in enum but never written); COMP-43 CoreNotificationConsumer (`channel_type=CORE_EVENTS`; created_by/updated_by=`PCSECRTC`); **COMP-51 CaseExpiryProcessor** (`channel_type=EXPIRY_BATCH`, created_by=`WCSEEXPB`, status=ERROR direct on retry exhaustion — no transition through PUBLISHED→FAILED) | Consumer-side audit, idempotency, predecessor blocking, and retry management. Shared across **five** writers via `channel_type` discriminator. Key cols: id (seq PK), i_case, i_action_seq, channel_type (EXPIRY_EVENTS / GP_EVENTS / BEN_EVENTS / CORE_EVENTS / EXPIRY_BATCH), idempotency_id (typed **String** — cross-table type inconsistency with `chbk_outbox_row` UUID), event_timestamp, status (PUBLISHED / SUCCESS / FAILED / ERROR / PENDING_DEFERRED / SKIPPED), retry_count, next_retry_at, error_code, error_message, original_event (JSON), created_by, created_at, updated_at. | COMP-12 InboundDisputeEventScheduler (Scheduler3 reads FAILED and PENDING_DEFERRED rows for retry — does NOT read PUBLISHED, SUCCESS, or SKIPPED rows; channel_type filter behaviour is OQ-COMP41-1). ⚠️ **No platform component currently identified as consumer of EXPIRY_BATCH rows** (COMP-51 writes status=ERROR direct; Scheduler3 reads FAILED/PENDING_DEFERRED only) — COMP-51 outbox rows may be terminal-write-only. Architect decision required. | ✅ Confirmed from COMP-17, COMP-41, COMP-42, COMP-43, COMP-51 audits |

**`wdp.outgoing_event_outbox` — writer-specific notes:**

- 🔴 **Correction:** COMP-18 NotificationOrchestrator was previously documented as a writer of this table. Source grep confirms zero references in the `wdp-outgoing-consumer` repo. **COMP-18 has been removed from the writer list.** COMP-18's writes land exclusively on `wdp.bre_orchestration_outbox` and `wdp.file_generation_event` (see rows below).
- **COMP-43 lifecycle detail:**
  - `created_by` / `updated_by` hardcoded to `"PCSECRTC"` on every write
  - `next_retry_at` is set on every non-SUCCESS update (not only on FAILED)
  - `retry_count` is incremented only on incoming `status==FAILED`; ERROR escalation fires on the third such write
  - `original_event` column carries the full `OutgoingEvent` JSON, including fields not used by COMP-43 (`actionStatus`, `expirationDate`, `responseDueDate`, `dateReceivedByAcquirer`, `level1Entity`–`level5Entity`, `disputeStage`, `channelType`)
  - ⚠️ **No Kafka-metadata write-back columns** (`kafka_topic`, `kafka_partition`, `kafka_offset` not present on the entity) — incident correlation between consumer logs and outbox rows requires log-side join
  - Idempotency check SELECT and INSERT run in **separate JPA short transactions** (`processNewCaseActionEvent` not `@Transactional`); race window is wider than v1.0 implied
- **COMP-42 lifecycle detail:**
  - `channel_type=BEN_EVENTS`; `created_by` / `updated_by` constant = `WBENC`
  - Status set used: PUBLISHED / SUCCESS / FAILED / ERROR / PENDING_DEFERRED. SKIPPED is defined in the enum but never written by COMP-42.
  - Predecessor scope is `caseNumber + channel_type=BEN_EVENTS + id < currentId`, applied as **in-memory filter after `findByCaseNumber` returns ALL channel_types** — same scope-pattern class as COMP-17 (predecessor scope is per-channel, not per-action_seq). Cross-action interference within the BEN_EVENTS channel is therefore present.
- **COMP-51 lifecycle detail:**
  - `channel_type=EXPIRY_BATCH`; `created_by=WCSEEXPB`
  - Writes `status=ERROR` directly on retry exhaustion — no PUBLISHED→FAILED transition
  - **Channel discriminator isolation maintained** — COMP-51 does not read other channel_types and is not read by Scheduler3 because Scheduler3 reads only FAILED/PENDING_DEFERRED. COMP-51's EXPIRY_BATCH rows are effectively terminal write-only until a consumer is defined.
- **COMP-17 lifecycle detail:**
  - No service-level `@Transactional` on `OutgoingEventOutboxServiceImpl` — every save runs in the implicit per-call JPA transaction
  - Predecessor candidate filter excludes `SUCCESS` and `SKIPPED`
  - Predecessor scope is `caseNumber + channelType=EXPIRY_EVENTS + id < currentId` only — **not scoped on `i_action_seq`**, causing cross-action interference (a stuck `ERROR`/`FAILED` row for action A on case X blocks all subsequent EXPIRY_EVENTS for the same case regardless of `action_seq`)
  - Status mapping: any non-ERROR predecessor (FAILED, PENDING, PENDING_DEFERRED, BLOCKED, PUBLISHED) maps the current row to PENDING_DEFERRED
- **COMP-41 lifecycle detail:**
  - Writes via independent auto-commit JPA saves — zero `@Transactional` annotations across `src/`. SELECT-before-INSERT duplicate check is therefore **not atomic**.
  - `idempotency_id` typed as `String` on its entity — same as COMP-17, COMP-42, COMP-43; cross-table type inconsistency with `chbk_outbox_row` (UUID) confirmed across all four writers
  - **Three distinct PUBLISHED-orphan paths**, all unrecoverable in-component, all invisible to COMP-12 Scheduler3 if Scheduler3 reads only FAILED / PENDING_DEFERRED: (a) post-ACK crash before Signifyd response; (b) Signifyd empty body ("NO_DATA_FROM_SIGNIFYD") — no status transition; (c) final outbox UPDATE failure after ACK
- **No DB UNIQUE constraint visible in any of COMP-17, COMP-41, COMP-42, COMP-43, or COMP-51 repos** — DBA confirmation required for `(idempotency_id, channel_type, event_timestamp)` enforcement.

---

| Table | Owning Component(s) | Purpose | Key Columns | Accessed Read-Only By | Confirmed |
|-------|---------------------|---------|-------------|----------------------|-----------|
| `wdp.bre_orchestration_outbox` | COMP-18 NotificationOrchestrator (`component=NOTIFICATION_ORCHESTRATOR` rows — created_by/updated_by=`WDPOUCU`; **no `@Transactional` on any write**; **no `SELECT FOR UPDATE`**; **four distinct write points** — Step 3a ERROR, Step 3d PUBLISHED, Step 6 ERROR/PENDING_DEFERRED, Step 7e SUCCESS/FAILED — partial-success state is the normal terminal state, not an exception); COMP-12 InboundDisputeEventScheduler (`component=BUSINESS_RULES` rows via Scheduler4) | Outbox for BRE and notification orchestration events. `component` discriminator field determines routing. Key cols: id, idempotency_id, component (NOTIFICATION_ORCHESTRATOR / BUSINESS_RULES), event_timestamp, status (PUBLISHED/FAILED/ERROR/PENDING_DEFERRED/SUCCESS), i_case, i_action_seq, target_action, published_action, retry_count, error_code, error_message, next_retry_at, original_event (JSON). ⚠️ PUBLISHED-status rows have **no automatic re-drive path** (Scheduler4 reads only FAILED/PENDING_DEFERRED) — manual intervention required. | COMP-12 InboundDisputeEventScheduler (Scheduler4 reads FAILED and PENDING_DEFERRED rows only) | ✅ Confirmed from COMP-18 source |
| `wdp.file_generation_event` | COMP-18 NotificationOrchestrator (sole writer — created_by/updated_by=`WDPOUCU`; INSERT-only; status hardcoded `STAGED` at entity field initialiser; columns left NULL on INSERT: `batch_id`, `next_retry_at`, `error_code`, `error_message`) | Staging table for file-based output requests. Written when Filter 4 routing matches. One row per document name. Key cols: idempotency_id, i_case, i_action_seq, c_case_ntwk, c_acq_platform, c_ntwk_case_id, document_name, file_type (AMEX/AMEX_HYBRID/DISCOVER/DISCOVER_RMO/BJS_PLCC/ISSUER_DOCS), status. ⚠️ Previously documented as `wdp.file_notifications` — that name does not exist in the codebase; correction confirmed by COMP-18 v2.0 audit. | COMP-45 CapitalOneResponseFileProcessor, COMP-46 NetworkResponseFileProcessor, COMP-47 DialoguIssuerDocumentProcessor (read to generate outbound files — not yet documented) | ✅ Confirmed from COMP-18 source |
| `wdp.br_case_audit_log` | COMP-16 BusinessRulesProcessor | Audit log of business rules evaluated (matched and not matched) for each US-path (CORE/VAP/PIN) message. Written on every message regardless of rule match outcome. **Standalone JPA transaction** — no `@Transactional` on `saveAuditLog`; implicit per-`saveAll` JPA transaction. Sequence `wdp.br_case_audit_log_id_seq`, allocationSize=1. **No unique constraint declared** — duplicate rows possible if DEC-005 pre-ACK is ever remediated without adding dedup. Key cols: id (seq), i_case, c_action_seq, rule_grp_name, rule_id, rule_name, is_valid, created_at | COMP-31 BusinessRulesService (read-only — GET /audit-log/case/{caseNumber} endpoint) | ✅ Confirmed from COMP-16, COMP-31 audits |
| `wdp.display_codes` | DBA scripts / database migrations (not written at runtime by any WDP service) | Reference data — maps internal code values to human-readable short and long descriptions by code domain type and platform. ~40 code domain types. Key cols: i_display_code (PK, seq), c_type (domain), c_code, c_short_desc, c_long_desc, c_platform. Cached in-memory by COMP-28 at JVM startup (no TTL, no eviction). | COMP-28 DisplayCodeService (read-only at runtime); COMP-31 BusinessRulesService (read-only — criteria enrichment on GET /criteria) | ✅ Confirmed from COMP-28 analysis |
| `wdp.dispute_static_tabs_rules` | DBA scripts / database migrations | UI tab permission rules — determines which portal tabs are visible based on user role. Read by COMP-28 on POST /search to resolve userPermission object in response. ⚠️ Two repository projections — `findByRoles` (used by POST /search) returns 11 Y/N flag columns; `getPermissions` (used by GET /privileges) returns 17 Y/N flag columns. The 6-column delta (`fax_match`, `fax_report`, `trans_detail`, `auth_detail`, `settle_detail`, `dispute_history`) is the source of the COMP-28 permission-shape divergence between endpoints. | COMP-28 DisplayCodeService (read-only) | ✅ Confirmed from COMP-28 analysis |
| `wdp.rules` | COMP-31 BusinessRulesService | Core rule records for US platform (PIN/CORE/VAP/LATAM). Mirror of nap.rules in the wdp schema. Key cols: id (PK, seq), group_id, group_name, name, type, description, sort_order, is_enabled, criteria_summary, action_summary, created_by, created_at, updated_by, updated_at | COMP-16 BusinessRulesProcessor (direct JPA read — bypasses BusinessRulesService REST entirely) | ✅ Confirmed from COMP-31, COMP-16 audits |
| `wdp.rule_criterion` | COMP-31 BusinessRulesService | Per-rule criterion instances for US platform. OneToMany from wdp.rules. CascadeType.ALL. Key cols: id, rule_id (FK), type, category, name, value, operator_symbol, created_at | COMP-16 BusinessRulesProcessor (direct JPA read, eager-loaded with wdp.rules) | ✅ Confirmed from COMP-31, COMP-16 audits |
| `wdp.rule_action` | COMP-31 BusinessRulesService | Per-rule action instances for US platform. OneToMany from wdp.rules. CascadeType.ALL. Key cols: id, rule_id (FK), category, action_name, field_name1/2/3, field_value1/2/3, created_at | COMP-16 BusinessRulesProcessor (direct JPA read, eager-loaded with wdp.rules) | ✅ Confirmed from COMP-31, COMP-16 audits |
| `wdp.rule_group` | ⚠️ Owner TBC — not determinable from COMP-31 source (possibly COMP-32 RulesService or DBA scripts) | Rule group definitions for US platform. Reference/configuration data. Key cols: id, name, description, triggered_by, type, is_enabled | COMP-31 BusinessRulesService (read-only — validates group on rule create, returns group list on GET /rule-group) | ⚠️ PENDING — confirm write owner |
| `wdp.FAX_ACTION` | COMP-29 FaxQueueService | Audit records of all fax operations (claim, reject, discard) performed by operations staff via Ops Portal. Written to WDP PostgreSQL by FaxQueueService when operating on legacy MS SQL Server fax data. | None confirmed | ✅ Confirmed from COMP-29 analysis |
| `wdp.acl` | COMP-02 UAMS | Access control list linking consumers to source systems and entity values. For downstream consumer authorization — not used by UAMS for its own auth. Key cols: i_acl_id (PK), c_consumer_name, c_source_system, c_entity_class, c_entity_type, c_entity_value, c_status, c_created_by, t_created_timestamp, c_updated_by, t_updated_timestamp. Index: acl_search_index on (c_consumer_name, c_status). DBA confirmation required for UNIQUE on `(c_consumer_name, c_source_system, c_entity_type, c_entity_value)` (OQ-COMP-02-12). | ⚠️ Downstream consumers TBC (OQ-COMP-02-5) | ✅ Confirmed from COMP-02 analysis |
| `wdp.api_route` | ⚠️ Owner TBC — write owner still requires team confirmation | API Gateway URL routing configuration. 26+ routes loaded at pod startup. Column set: id, path, original_path_regex, replacement_path, uri, auth_exceptions. | COMP-01 API Gateway (read-only at startup) | ✅ Column set confirmed from `ApiRoute.java` (COMP-01 v2.0 audit). Write owner still TBC — team confirmation required. |
| `wdp.pin_case_i_case_sequence` | COMP-23 CaseManagementService | PostgreSQL sequence for case-number generation. Shared by PIN, CORE, VAP, LATAM. Consumed OUTSIDE any `@Transactional` — sequence values are consumed on rollback. | None confirmed | ✅ Confirmed from COMP-23 audit |
| `wdp.queues` | COMP-30 UserQueueSkillService | Queue definitions for US-platform internal search and routing. Written via service-level `@Transactional` on `createQueue` / `updateQueue` bound to `@Primary usTransactionManager` (US datasource — outer TX binds correctly). | ⚠️ TBC | COMP-27 CaseSearchService (read-only via `JdbcClient` native SQL) | ✅ Confirmed from COMP-30 audit |
| `wdp.queue_criterion` | COMP-30 UserQueueSkillService | Queue filter rows (column / operator / value) applied as SQL WHERE conditions for US-platform search. Service-level `@Transactional` binds correctly via @Primary usTransactionManager. | ⚠️ TBC | COMP-27 CaseSearchService (read-only via `JdbcClient` native SQL) | ✅ Confirmed from COMP-30 audit |
| `wdp.user_queue` | COMP-30 UserQueueSkillService | User-to-queue mapping for US platform. Service-level `@Transactional` binds correctly via @Primary usTransactionManager. | ⚠️ TBC | COMP-27 CaseSearchService (read-only — queue-filtered search) | ✅ Confirmed from COMP-30 audit |
| `wdp.hash_key_store` | COMP-35 EncryptionService | EPAN→HPAN→ciphertext mapping store. INSERT on encrypt-miss; UPDATE `last_seen_at` on encrypt-hit and on every decrypt. Sole reader is also COMP-35 — ciphertext never leaves this service. Transaction manager: `wdpTransactionManager`. ⚠️ No JPA `unique=true` on `hpan`. DB-level UNIQUE existence not determinable from repo (no migration scripts present). Key cols: `id` (PK seq), `hpan` (64-char hex), `pan_ct` (Base64 IV‖ciphertext‖GCM-tag), `dek_id` (FK → `wdp.data_enc_key`), `inserted_at`, `last_seen_at`. | None other than COMP-35 | ✅ Confirmed from COMP-35 audit |
| `wdp.data_enc_key` | COMP-35 EncryptionService | KMS-wrapped Data Encryption Key registry. INSERT on DEK rotation; SELECT-by-id on decrypt; SELECT-top-by-inserted_at-desc on rotation. ⚠️ `insertEncryptedDEK` runs in a Spring-Data default short transaction — separate from the surrounding `rotateDEK()` orchestration. Multi-pod DEK row proliferation is uncontrolled (no distributed lock). Key cols: `dek_id` (PK seq `wdp.hash_key_store_dek_id_seq`), `dek_enc` (Base64 KMS-wrapped DEK), `inserted_at`. | None other than COMP-35 | ✅ Confirmed from COMP-35 audit |

> ⚠️ **Removed from this section:** `wdp.dispute_event_change_log`. Source grep confirms zero references to this table in the `mdws-gcp-case-management-service` (COMP-23) and `gcp-cas-actions-service` (COMP-24) repos. The v1.0 DRAFT claim of a cross-datasource audit write from COMP-23's NAP create path is not supported by source and is withdrawn. COMP-24's `EventChangeLogRepository` does not exist in source. If this table is still active in production, its writers live outside both component repos — investigation required outside this reconciliation.

#### `wdp` schema — Reference rule tables consumed by COMP-32 RulesService

**All 15 tables below**: writers are by DBA scripts / rule-administration tooling — write owner remains TBC. COMP-32 RulesService is the runtime READ-ONLY consumer for each. Some are also read by COMP-16 BusinessRulesProcessor for rule evaluation (per established `wdp.rules` / `wdp.rule_criterion` / `wdp.rule_action` pattern).

| Table | Owning Component(s) | Purpose (per COMP-32 endpoint surface) | Accessed Read-Only By | Confirmed |
|-------|---------------------|----------------------------------------|----------------------|-----------|
| `wdp.dispute_action_rules` | ⚠️ Owner TBC (DBA / rule-admin tooling) | Action-rules driving COMP-32 GET /dispute-action-rules | COMP-32 RulesService | ✅ Reader confirmed from COMP-32 audit |
| `wdp.business_event_rules` | ⚠️ Owner TBC | Business-event rules driving COMP-32 GET /business-event-rules | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.dispute_accept_item_rules` | ⚠️ Owner TBC | Accept-item rules for COMP-32 dispute-accept-rules endpoint | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.dispute_new_action_rules` | ⚠️ Owner TBC | New-action rules for COMP-32 | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.dispute_new_case_action_respond_rules` | ⚠️ Owner TBC | New-case-action-respond rules for COMP-32 | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.dispute_first_action_rules` | ⚠️ Owner TBC | First-action rules for COMP-32 | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.dispute_case_exp_rules` | ⚠️ Owner TBC | Case-expiry rules consumed by COMP-32 | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.pre_action_status_rules` | ⚠️ Owner TBC | Pre-action-status rules for COMP-32 | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.disputes_visa_queue_status_rules` | ⚠️ Owner TBC | Visa queue-status rules for COMP-32 | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.notification_rules` | ⚠️ Owner TBC | Notification rules for COMP-32 | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.nap_chbk_response_rules` | ⚠️ Owner TBC | NAP chargeback response rules for COMP-32 | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.workflow_rules` | ⚠️ Owner TBC | Workflow rules for COMP-32 | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.dispute_doc_details_type_rules` | ⚠️ Owner TBC | Document-detail-type rules for COMP-32 | COMP-32 RulesService | ✅ Reader confirmed |
| `wdp.document_type_rules` | ⚠️ Owner TBC | Document-type rules — endpoint is disabled, table still queryable | COMP-32 RulesService (table still mapped though endpoint disabled) | ✅ Reader confirmed |
| `wdp.dispute_update_event_log_rules` | ⚠️ Owner TBC | Dispute-update-event-log rules — endpoint is disabled, table still queryable | COMP-32 RulesService (table still mapped though endpoint disabled) | ✅ Reader confirmed |

---

### Schema: `nap`

*NAP acquiring platform schema. Managed by WDP team for NAP integration.*

| Table | Owning Component(s) | Purpose | Key Columns | Accessed Read-Only By | Confirmed |
|-------|---------------------|---------|-------------|----------------------|-----------|
| `nap.nap_parent_entity` | COMP-02 UAMS | Top-level merchant groupings for NAP platform. ⚠️ DEC-021 wrong-TM occurrences (3 of 7): `AccessManagementServiceImpl.createEntity`, `AccessManagementServiceImpl.updateEntity`, `AccessManagementDaoImpl.deleteParentEntity` — all use `wdpTransactionManager` instead of `napTransactionManager`. | entity_id, name (UNIQUE — DBA confirmation pending), status | ⚠️ TBC | ✅ Confirmed from COMP-02 audit |
| `nap.nap_child_entity` | COMP-02 UAMS | Sub-groupings beneath a NAP parent entity. ⚠️ DEC-021 wrong-TM occurrences (3 of 7): `MerchantServiceImpl.saveChildWithMerchant`, `AccessManagementServiceImpl.createEntity`, `AccessManagementServiceImpl.updateEntity`, `AccessManagementDaoImpl.deleteChildEntity`, `AccessManagementDaoImpl.deleteParentEntity`. | child_id, parent_id (FK → nap.nap_parent_entity), name (UNIQUE composite with i_parent_entity_id — DBA pending), status | ⚠️ TBC | ✅ Confirmed from COMP-02 audit |
| `nap.nap_merchant` | COMP-02 UAMS | Individual merchant IDs with MCC and WPG ID. ⚠️ DEC-021 wrong-TM occurrences (4 of 7): `MerchantServiceImpl.saveChildWithMerchant`, `MerchantDaoImpl.deleteMerchantByParent`, `AccessManagementDaoImpl.deleteChildEntity`, `AccessManagementDaoImpl.deleteParentEntity`. | mid, mcc, wpg_id, parent_id | ⚠️ TBC | ✅ Confirmed from COMP-02 audit |
| `nap.nap_entity_rel` | COMP-02 UAMS | Merchant-to-entity relationships — primary NAP authorization lookup. ⚠️ DEC-021 wrong-TM occurrences (5 of 7): `MerchantServiceImpl.saveChildWithMerchant`, `MerchantDaoImpl.updateMerchantRelationships`, `MerchantDaoImpl.deleteMerchantByParent`, `AccessManagementDaoImpl.deleteChildEntity`, `AccessManagementDaoImpl.deleteParentEntity`. UNIQUE on `(i_parent_entity_id, i_child_entity_id, i_mid)` — DBA confirmation pending. | merchant_id, parent_entity, child_entities | COMP-02 UAMS /authorize endpoint | ✅ Confirmed from COMP-02 audit |
| `nap.case` | COMP-23 CaseManagementService (primary), COMP-24 CaseActionService, COMP-27 CaseSearchService (lock-column UPDATE only — `c_case_lock`, `c_locked_by`, `x_locked_at`, `x_updt`, `z_updt`. No `@Transactional`, no `SELECT FOR UPDATE`, auto-commit JDBC) | NAP dispute case record. Mirror of wdp.CASE on the NAP schema. Key cols: I_CASE (PK / NAP-prefixed caseNumber), C_CASE_STA, C_CASE_NTWK, C_CASE_LOCK, C_LOCKED_BY, **X_LOCKED_AT** (NAP-side lock-timestamp drift vs wdp.CASE.Z_LOCKED_AT), I_ACCT_CDH (⚠️ clear PAN written by COMP-23 on standard create — DEC-004 violation). | COMP-02 UAMS (read-only — `/authorize` endpoint,), COMP-16 BusinessRulesProcessor (read-only — UK rule evaluation), COMP-22 DisputeService (read-only when `coreMigrationStatus=true`), COMP-25 NotesService (read-only — case context), COMP-27 CaseSearchService (read-only) | ✅ Confirmed from COMP-02, COMP-15, COMP-22, COMP-23, COMP-24, COMP-25, COMP-27 audits |
| `nap.action` | COMP-23 CaseManagementService (insert + update — same pattern as wdp.ACTION); COMP-24 CaseActionService (insert + update — ⚠️ NAP vs US functional asymmetry on EP 5: `UKCaseActionDaoImpl.updateAction` does not process `chbkOutbox` on any branch — NAP silently ignores `chbkOutbox` field that US handles on six branches) | NAP case action records. Mirror of wdp.ACTION. | COMP-06 NAPDisputeDeclineBatch (READ-ONLY — confirmed source-verified; consumer of decline-eligible NAP actions for the decline batch), COMP-16 BusinessRulesProcessor (read-only — UK rule evaluation), COMP-22 DisputeService (read-only when `coreMigrationStatus=true`), COMP-27 CaseSearchService (read-only), COMP-30 UserQueueSkillService (read-only — confirmed source-verified) | ✅ Confirmed from COMP-06, COMP-22, COMP-23, COMP-24, COMP-27, COMP-30 audits |
| `nap.NOTES` | COMP-25 NotesService (primary — via `napTransactionManager`); COMP-23 CaseManagementService (NAP case create path conditional); COMP-24 CaseActionService (conditional — when note field present in request, same transaction as action write) | Dispute case notes for NAP/UK platform. Separate `napTransactionManager` — never mixed with wdp schema. Identical column structure to `wdp.NOTES`. Key cols: I_NOTE_ID (PK, seq), I_CASE, I_ACTION_SEQ, C_NOTE_TYPE, T_NOTE, D_NOTE, X_INSRT, Z_INSRT, X_INSRT_DISPLAY, X_UPDT_DISPLAY. ⚠️ Dual-writer fact (COMP-25 + COMP-23 NAP create path) — Section 4 entry not yet added; consider adding equivalent shared-table treatment for `nap.NOTES` at next reconciliation. | ⚠️ Readers TBC | ✅ Confirmed from COMP-23, COMP-24, COMP-25 audits |
| `nap.br_case_audit_log` | COMP-16 BusinessRulesProcessor | Audit log of business rules evaluated (matched and not matched) for each UK/NAP path message. **Standalone JPA transaction** — no `@Transactional` on `saveAuditLog`; implicit per-`saveAll` JPA transaction. Sequence `nap.br_case_audit_log_id_seq`, allocationSize=1. **No unique constraint declared.** Key cols: id (seq), i_case, c_action_seq, rule_grp_name, rule_id, rule_name, is_valid, created_at | COMP-31 BusinessRulesService (read-only — GET /audit-log/case/{caseNumber} endpoint) | ✅ Confirmed from COMP-16, COMP-31 audits |
| `nap.rules` | COMP-31 BusinessRulesService | Core rule records for NAP/UK platform. Key cols: id (PK, seq), group_id, group_name, name, type, description, sort_order, is_enabled, criteria_summary, action_summary, created_by, created_at, updated_by, updated_at | COMP-16 BusinessRulesProcessor (direct JPA read — bypasses BusinessRulesService REST entirely at execution time) | ✅ Confirmed from COMP-31, COMP-16 audits |
| `nap.rule_criterion` | COMP-31 BusinessRulesService | Per-rule criterion instances for NAP platform. OneToMany from nap.rules. Key cols: id, rule_id (FK → nap.rules.id), type, category, name, value, operator_symbol, operator_readable, description, created_at | COMP-16 BusinessRulesProcessor (direct JPA read, eager-loaded with nap.rules at execution) | ✅ Confirmed from COMP-31, COMP-16 audits |
| `nap.rule_action` | COMP-31 BusinessRulesService | Per-rule action instances for NAP platform. OneToMany from nap.rules. Key cols: id, rule_id (FK), category, action_name, field_name1/2/3, field_value1/2/3, field_value1/2/3_description, created_at | COMP-16 BusinessRulesProcessor (direct JPA read, eager-loaded with nap.rules at execution) | ✅ Confirmed from COMP-31, COMP-16 audits |
| `nap.rule_group` | ⚠️ Owner TBC — not determinable from COMP-31 source (possibly COMP-32 RulesService or DBA scripts) | Rule group definitions for NAP platform. Reference/configuration data. Key cols: id, name, description, triggered_by, type, is_enabled, c_display_group_name | COMP-31 BusinessRulesService (read-only — validates group on rule create, returns group list on GET /rule-group) | ⚠️ PENDING — confirm write owner |
| `nap.rule_criteria` | ⚠️ Owner TBC — NOT determinable from COMP-31 source | Reference/lookup table for available criterion definitions. Distinct from nap.rule_criterion (which holds per-rule instances). Read only by COMP-31 for GET /criteria endpoint. | id, category, type, criteria definition fields | COMP-31 BusinessRulesService (read-only) | ⚠️ PENDING — confirm write owner |
| `nap.rule_action_field` | ⚠️ Owner TBC — NOT determinable from COMP-31 source | Reference/lookup table for available action field definitions. Read only by COMP-31 for GET /actions endpoint. | id, category, action_name, action_display_name, field_name, field_display_name, type, is_required | COMP-31 BusinessRulesService (read-only) | ⚠️ PENDING — confirm write owner |
| `nap.dispute_nap_new_case_action_rules` | ⚠️ Owner TBC (DBA / rule-admin tooling) | NAP-specific new-case-action rules — only `nap.*` table read by COMP-32. | ⚠️ TBC | COMP-32 RulesService (read-only) | ⚠️ PENDING — confirm write owner |
| `nap.queues` | COMP-30 UserQueueSkillService | Queue definitions and criteria summary for NAP internal search. ⚠️ Service-level `@Transactional` on `createQueue` / `updateQueue` binds to `@Primary usTransactionManager` — **UK writes NOT covered by the outer TX** (per-repository implicit TX only). DEC-021 wrong-TM occurrence on this table. | ⚠️ TBC | COMP-27 CaseSearchService (read-only via `JdbcClient` native SQL) | ✅ Confirmed from COMP-30 audit. Cross-component grep for other writers still pending. |
| `nap.queue_criterion` | COMP-30 UserQueueSkillService | Queue filter rows (column / operator / value) applied as SQL WHERE conditions for NAP internal regular-user search. ⚠️ The `RECEIVED_DOCUMENT` enum returns a literal SQL identifier with a space character — emits invalid SQL at runtime (defect, COMP-27). ⚠️ The `value` column content is concatenated into SQL by `QueueSupport.buildQueueCriterion` — latent SQL-injection surface (column and operator are enum-whitelisted; value is not). ⚠️ Same DEC-021 wrong-TM concern as `nap.queues`. | column, operator, value | COMP-27 CaseSearchService (read-only via `JdbcClient` native SQL) | ✅ Confirmed from COMP-30 audit |
| `nap.users` | COMP-02 UAMS (primary); COMP-30 UserQueueSkillService (POST /user side-effect — auto-enrol writes a user row. ⚠️ POST /user has no `@Transactional` — auto-enrol runs in a separate implicit transaction; failure leaves orphaned user row.) | NAP user lookup | ⚠️ TBC | COMP-27 CaseSearchService (read-only — NAP internal regular-user queue filtering) | ✅ Confirmed reader from COMP-27 audit; COMP-30 write side-effect confirmed |
| `nap.user_queue` | COMP-02 UAMS; COMP-30 UserQueueSkillService (writes via createQueue / updateQueue side effects on the NAP datasource — same wrong-TM pattern as `nap.queues`) | User-to-queue mapping | ⚠️ TBC | COMP-27 CaseSearchService (read-only — queue-filtered search) | ✅ Confirmed reader from COMP-27 audit; COMP-30 writer confirmed |
| `NAP.DISPUTE_EVENT_CONSUMER_ERROR` | COMP-05 NAPDisputeEventProcessor (primary; owns DDL pattern); COMP-23 CaseManagementService (⚠️ **NAP create path blind-merge** — calls `.save()` on a freshly-constructed `ConsumerErrorEntity` with three fields populated, without a prior `findById` guard. JPA merge INSERTs a sparse row when the id does not already exist. Cross-component write into a COMP-05-owned table with no owner check.); COMP-24 CaseActionService (insert-path conditional — outbox-style UPDATE inside the same `@Transactional` as the domain write); **COMP-39 NAPOutcomeProcessor** (writes outbound-delivery error rows with `C_ACQ_PLATFORM=NAP`, `C_EVENT_TYPE in {OUT_SRV118, OUT_SRV117}`; also reads back the table on every Kafka message via prior-error scan) | Database DLQ for failed Kafka events on nap-dispute-events. ⚠️ **PCI-DSS storage finding (HIGH):** the entity has a `cvv` field mapped to a `C_CVV` column, AND the raw `NotificationEvent` JSON is persisted into `C_KAFKA_EVENT` which also contains the `cvv` field. CVV is therefore persisted twice per error row. Cross-link: COMP-04 logs the CVV; COMP-05 persists it. ⚠️ **No DB-level UNIQUE constraint visible** (no `@UniqueConstraint`, no `@Index`, no `@Column(unique=true)` on the entity; DDL not in repo). Key cols: error_id, raw_payload, arn, error_timestamp, c_event_type, c_acq_platform, source_system_name, status (FAILED1 / FAILED2 / …), C_CVV, C_KAFKA_EVENT (raw JSON). | ⚠️ Manual reprocessing path only — but COMP-05 and COMP-39 BOTH read this table back on every Kafka message via prior-error scan filters that **omit `C_EVENT_TYPE`**, opening a cross-component reprocessing hazard. | ✅ Confirmed from COMP-05, COMP-23, COMP-24, COMP-39 audits |
| `NAP.BUSINESS_RULE_CONSUMER_ERROR` | COMP-05 NAPDisputeEventProcessor | Separate error table for business-rule-related processing failures, distinct from the primary `NAP.DISPUTE_EVENT_CONSUMER_ERROR`. No DDL in repo. | ⚠️ TBC — column list not yet captured | ⚠️ Manual reprocessing | ✅ Confirmed from COMP-05 audit |
| `NAP.NAP_UPDATE_RESPONSE_RULES` | ⚠️ Owner TBC — JPA entity declared in COMP-39 but no DDL/migration is present in COMP-39 repo. Likely owned by COMP-32 RulesService or DBA scripts. | Rule-driven outbound NAP update-response routing — read by COMP-39 on every outbound delivery decision. | ⚠️ TBC | COMP-39 NAPOutcomeProcessor (read-only) | ⚠️ PENDING — confirm write owner |
| `NAP.issuer_raised_dispute_timefrm_rules` | ⚠️ Owner TBC | Issuer-raised dispute timeframe rules. Accessed via `TimeframeRulesEntity` / `TimeframeRulesRepository`. | ⚠️ TBC | COMP-05 NAPDisputeEventProcessor (read-only) | ✅ Confirmed reader from COMP-05 audit |
| `NAP.mcm_queue_data` | ⚠️ Owner TBC | MCM queue working data — read by COMP-05 for prior-event correlation. Schema/name confirmed source-verified. | ⚠️ TBC | COMP-05 NAPDisputeEventProcessor (read-only) | ✅ Confirmed reader from COMP-05 audit |
| `nap.case_i_case_sequence` | COMP-23 CaseManagementService | PostgreSQL sequence for NAP case-number generation. Consumed OUTSIDE any `@Transactional` — sequence values are consumed on rollback. | None confirmed | ✅ Confirmed from COMP-23 audit |

---

### Schema: DynamoDB — `WDP_PIN_DISPUTE_DOCUMENTS` and `NAP_DISPUTE_DOCUMENTS`

| Entity | Owning Component(s) | Purpose | Key columns | Notes |
|--------|---------------------|---------|-------------|-------|
| `DocumentMetaData` (across both tables) | COMP-37 DocumentManagementService | Evidence document metadata | caseNumber + actionSequence (PK), C_STAGE_CODE, I_ACTION_SEQ, C_DOC_TYPE, N_DOC_NAME, Z_UPDT | Five GSIs declared per table (`C_STAGE_CODE`, `I_ACTION_SEQ`, `C_DOC_TYPE`, `N_DOC_NAME`, `Z_UPDT`) — none queried from Java. GSI projection type not determinable from source — confirm via AWS console / Terraform / CloudFormation. No PAN attribute on the entity (DEC-019 compliant). |
| `QuestionnaireEntity` (WDP/US datasource — RDBMS, NOT DynamoDB) | COMP-37 DocumentManagementService | Questionnaire response store written by Endpoint 11 questionnaire path. UPSERT semantics — last-write-wins on `(caseNumber, actionSequence)` PK. `@Transactional(rollbackOn=Exception.class)` — stronger atomicity than other publishers on `business-rules` topic. No unique constraint beyond PK. | caseNumber + actionSequence (PK) | ⚠️ Co-owner with COMP-26 QuestionnaireService — see `wdp.disputes_questionnaire` row above. Cross-component ownership review needed to confirm whether the COMP-37 entity name `QuestionnaireEntity` maps to the same physical table as COMP-26's `wdp.disputes_questionnaire`. |

---

### Spring Batch Metadata Tables

*Written by all Spring Batch jobs. Schema and prefix vary by component — injected via K8s secrets.*

| Table | Written by | Purpose |
|-------|------------|---------|
| `BATCH_JOB_INSTANCE` | COMP-06, COMP-07, COMP-08, COMP-09, COMP-51 | Job identity and deduplication |
| `BATCH_JOB_EXECUTION` | COMP-06, COMP-07, COMP-08, COMP-09, COMP-51 | Execution status per run |
| `BATCH_STEP_EXECUTION` | COMP-06, COMP-07, COMP-08, COMP-09, COMP-51 | Step-level progress and counts |

**Spring Batch DDL ownership:**
- COMP-06, COMP-07, and COMP-08 all set `spring.batch.jdbc.initialize-schema = never` and `spring.jpa.hibernate.ddl-auto = false` — Spring Batch metadata tables must pre-exist. DDL is not managed by any of these batches.
- ⚠️ Schema location (which PostgreSQL database and schema) for Spring Batch metadata tables varies per component and must be confirmed from each component's source analysis. Table prefix injected via K8s secret (`${table_prefix}` for COMP-06; equivalent for others). Production schema for the env-driven `${table_prefix}BATCH_*` tables is not in any of these repos — DBA confirmation required.

---

## 3. External Database Dependencies

### IBM DB2 — Core Platform (BC schema) — WDP Writes

COMP-43 CoreNotificationConsumer is the **sole WDP component that writes to IBM DB2**. All other WDP components that access DB2 are read-only.

| Table | WDP Writer | Purpose | Key Columns | Notes |
|-------|-----------|---------|-------------|-------|
| `BC.TBC_DM_CASE` | COMP-43 CoreNotificationConsumer | Parent dispute case record for CORE platform. One row per WDP case. | I_CASE_ID (IDENTITY PK generated by DB2), I_CASE (patched = leftPad(I_CASE_ID, 10, "9")), c_wdp_case (WDP case number backlink), **I_ACCT_CDH**, **I_ACCT_CDH_LST** (PAN last-4) | **Two-phase save mechanism applies only to `saveCoreCase` (CREATE + actionSeq=01):** first save generates I_CASE_ID via IDENTITY; second save patches `I_CASE = leftPad(I_CASE_ID, 10, "9")`. **`addOccurrence` is single save.** **UPDATE path is single save outside any enclosing `@Transactional`.** DB2 reads use WITH UR (uncommitted read isolation). |
| `BC.TBC_DM_OCCUR` | COMP-43 CoreNotificationConsumer | Action/occurrence records as children of TBC_DM_CASE | I_OCCUR_ID (IDENTITY PK), I_CASE_ID (FK), I_CASE_OCCUR (occurrence number / actionSequence) | Same `@Transactional(coreTransactionManager)` as TBC_DM_CASE on CREATE-first only. INSERT on CREATE, UPDATE on UPDATE. ⚠️ Outbox INSERT on `wdpTransactionManager` commits **before** `coreDao.saveCoreCase` is invoked — DEC-001 deviation crash window confirmed wide. |
| `BC.TBC_DM_NOTES` | COMP-43 CoreNotificationConsumer | First note for a case/occurrence. Written only on CREATE + actionSequence=01 when notes list non-empty. | I_NOTE_ID (IDENTITY PK), I_CASE_ID (FK), I_OCCUR_ID | Same transaction as TBC_DM_CASE and TBC_DM_OCCUR. INSERT only. |

### IBM DB2 — Core Platform — Read-Only Access

| Table(s) | Accessed by | Purpose |
|----------|------------|---------|
| MD.TMD_ENTY, MD.TMD_ENTY_REL, DC.TDC_VIQ_ORG_ENTY, BC.TBC_MRCHNT_MAST_BO, MD.TMD_DISPLAY_CODES, MD.TMD_MRCHNT_WOMPLY, BC.TMD_CHAIN, MD.TMD_STORE, MD.TMD_MRCHNT, and others | COMP-03 CHAS | Entity scope validation and Core hierarchy data. **`MD.TMD_CHAIN_ANALY` confirmed as a declared-but-unused read target** via dead `ProductEntitlementRepository` injection — already noted in v1.0, reaffirmed in v1.1. |
| BC.TBC_DM_CASE, BC.TBC_DM_OCCUR | COMP-15 EvidenceConsumer (V3 path only — WITH UR isolation) | V3 legacy CORE path case lookup |
| BC.TBC_DM_CASE, BC.TBC_DM_OCCUR | COMP-22 DisputeService (when coreMigrationStatus=false; source-verified read-only on `coreTransactionManager` — zero writes) | Legacy CORE dispute summary aggregation |
| BC.TBC_CC_TR07, BC.TBC_MRCHNT_MAST_BO | COMP-34 MerchantTransactionService (CapOne enrichment only) | CapOne transaction and merchant lookup |
| **BC.TBC_DM_CASE only** | COMP-43 CoreNotificationConsumer (also writer — see above) | Step 7 UPDATE path (`findByCaseId`) and CREATE-subsequent path (`findByWdpCaseNumber`) only. WITH UR isolation. **Enrichment is fully REST-driven** — DB2 is not used for enrichment reads. |

### MS SQL Server — Legacy Fax System

| Table | Accessed by | Access type | Purpose |
|-------|------------|-------------|---------|
| `dbo.IncomingFaxes` | COMP-29 FaxQueueService | Read + conditional status update | Legacy fax queue. COMP-29 reads fax records, updates their status on claim/reject/discard. Shared with legacy fax processing systems outside WDP. Not WDP-owned. |

---

## 4. Shared Table Risk Register

Tables written by more than one component require coordination to avoid
race conditions, duplicate writes, or conflicting state.

| Table | Writers | Risk | Mitigation in place | Severity |
|-------|---------|------|---------------------|----------|
| `wdp.chbk_outbox_row` | COMP-07, COMP-08, COMP-09, COMP-11 (INSERT PENDING / SKIPPED rows); COMP-12, COMP-14, COMP-15, COMP-23, COMP-24 (status updates) | Multiple components write simultaneously. Batch jobs INSERT rows; consumers update status of pre-existing rows. **No DB unique constraint visible in any audited repo** — confirmed by COMP-07, COMP-08, COMP-11, COMP-13 source verification. **COMP-08 writer-ACK hazard** — items can be ACKed off MCM with no corresponding outbox row (silent data loss). **COMP-09 update-path always INSERTs new row** — table growth proportional to phase × rerun frequency. **COMP-23 update path has no terminal-status guard** — can overwrite a PROCESSED row. | Per-component duplicate check on (networkCaseId + networkPhaseId + disputeStage) before INSERT. Status updates operate on pre-existing rows by PK. **DB-level unique-index existence in live schema must be confirmed with the DBA team — open across COMP-07, COMP-08, COMP-11.** | 🟡 MEDIUM |
| `wdp.file_job` | COMP-11 FileProcessor (creates + updates status — never writes COMPLETED), COMP-12 (status→COMPLETED), COMP-13 (ack fields only) | Three components write to same row at different lifecycle stages. ⚠️ Cross-component break: COMP-11 never writes COMPLETED; COMP-13 polls for `status IN (COMPLETED, ERROR)` — ACK pipeline relies on COMP-12 actually flipping the status. Contract is undocumented in all three repos. | Non-overlapping field sets — COMP-11 owns most columns; COMP-12 owns `completed_at` and the COMPLETED transition; COMP-13 owns four ack columns enforced by `@DynamicUpdate`. | 🟢 LOW — field-level separation |
| `wdp.CASE` | COMP-23 CaseManagementService (primary), COMP-24 CaseActionService (open/close transitions), COMP-15 EvidenceConsumer (Z_UPDT conditional), COMP-27 CaseSearchService (lock-column UPDATE only), COMP-37 DocumentManagementService (column-level UPDATE on desk-blanking branch) | Multiple services update the central case record. **COMP-24 last-write-wins** — no `@Version`, no pessimistic lock, no advisory lock. **COMP-27 lock UPDATE is non-atomic** — pre-read and UPDATE are two separate auto-commit JDBC statements with no `SELECT FOR UPDATE`, no conditional UPDATE. **COMP-37 column-level UPDATE has no INSERT/DELETE owner identified** — COMP-22 confirmed source-verified zero writes, withdrawing the prior speculation that COMP-22 might be the INSERT/DELETE owner. **The COMP-37 INSERT/DELETE owner question remains open.** | COMP-23 uses case-level transactions. COMP-24 and COMP-15 updates are conditional and scoped. COMP-27 lock UPDATE is field-scoped (5 columns only). No optimistic locking confirmed. | 🟠 MEDIUM-HIGH — confirm locking strategy and INSERT/DELETE owner |
| `wdp.ACTION` | COMP-23 CaseManagementService (INSERT + UPDATE), COMP-24 CaseActionService (INSERT + UPDATE), COMP-15 EvidenceConsumer (UPDATE C_OWNR conditional) | Multiple services write action records. No RBAC enforcement in COMP-24 — any authenticated caller can modify any action. **COMP-24 open-action constraint enforced in-memory only** — race window on concurrent POSTs for same caseNumber. | No write-lock or optimistic lock confirmed from source. | 🔴 HIGH — no RBAC + concurrent write risk |
| `wdp.NOTES` | COMP-25 NotesService (primary), COMP-23 CaseManagementService (US create path duplicate-insert defect), COMP-24 CaseActionService (conditional) | Multiple services write to same table. **COMP-23 duplicate-insert defect inflates row count per case create — investigation needed on whether downstream readers deduplicate.** COMP-25 has no awareness of the co-writers. | None — defect not fixed in current working tree. | 🟠 MEDIUM-HIGH — duplicate row risk on COMP-23 US create path |
| `wdp.disputes_questionnaire` | COMP-26 QuestionnaireService (primary; **no `@Transactional` on any path** — TOCTOU race on PUT and B1 UPSERT paths), COMP-37 DocumentManagementService (UPSERT on Endpoint 11), COMP-15 EvidenceConsumer (upsert document names) | Three components write to same table. COMP-26 has a POST idempotency gap — duplicate POSTs insert new rows rather than updating, AND B1 UPSERT path is exposed to concurrent insert races (TOCTOU). COMP-37 last-write-wins on `(caseNumber, actionSequence)` PK. | No DB unique constraint confirmed on (I_CASE, I_ACTION_SEQ). | 🔴 HIGH — duplicate row risk on COMP-26 POST + TOCTOU on COMP-26 B1 + last-write-wins on COMP-37 |
| `wdp.case_expiry` | COMP-17 CaseExpiryUpdateConsumer (INSERT/UPSERT/DELETE on Kafka events), **COMP-51 CaseExpiryProcessor** (SELECT page-fetch, UPDATE i_retry_count, DELETE on success/CLOSED-action/retry-exhaustion) | **Shared write between COMP-17 and COMP-51 with no coordination mechanism.** COMP-17 upserts on Kafka events; COMP-51 reads, UPDATEs retry counter, and DELETEs. Race conditions possible between the two components on the same row (`i_retry_count` overwrite, mid-flight upsert during chunk processing). No row-level lock, no version column, no SELECT FOR UPDATE. | None — coordination strategy pending architect decision. | 🔴 HIGH — concurrent multi-writer with no locking |
| `wdp.outgoing_event_outbox` | COMP-17 (EXPIRY_EVENTS, created_by=`WCSEEXPC`), COMP-41 (GP_EVENTS, created_by=`WNEC`), COMP-42 (BEN_EVENTS, created_by=`WBENC`), COMP-43 (CORE_EVENTS, created_by=`PCSECRTC`), **COMP-51 (EXPIRY_BATCH, created_by=`WCSEEXPB`)** | **Five** writers writing to the same table using `channel_type` as discriminator. Each writer uses a distinct `channel_type` to partition its rows. **COMP-17 cross-action interference confirmed** (predecessor scope is `caseNumber + channelType + id < currentId` — not scoped on `i_action_seq`). **COMP-42 has the same scope-pattern** (caseNumber + BEN_EVENTS + id < currentId, in-memory filter after findByCaseNumber returns ALL channel_types). **COMP-41 has three distinct PUBLISHED-orphan paths** invisible to COMP-12 Scheduler3. **COMP-51 writes status=ERROR direct** (no PUBLISHED→FAILED transition); EXPIRY_BATCH rows have **no identified consumer** — Scheduler3 reads FAILED/PENDING_DEFERRED only. **No DB unique constraint visible in any of COMP-17, COMP-41, COMP-42, COMP-43, or COMP-51 repos.** | `channel_type` column discriminates between component domains. Each component only reads/writes its own `channel_type` rows — discriminator isolation maintained. **Whether `(idempotency_id, channel_type, event_timestamp)` is enforced at DB level must be confirmed with the DBA team.** | 🟢 LOW pending COMP-12 Scheduler3 channel_type-filter confirmation. **If Scheduler3 does not filter consistently, severity escalates to 🟡 MEDIUM platform-wide.** EXPIRY_BATCH terminal-write-only-consumer gap is logged as a separate architect decision — does not affect this severity rating. |
| `wdp.bre_orchestration_outbox` | COMP-18 NotificationOrchestrator (NOTIFICATION_ORCHESTRATOR rows, created_by=`WDPOUCU`), COMP-12 InboundDisputeEventScheduler (BUSINESS_RULES rows via Scheduler4) | Two components write to same table using `component` discriminator. COMP-18 has four distinct write points with no `@Transactional` grouping — partial-success state is the normal terminal state. PUBLISHED rows have no automatic re-drive path. | `component` column discriminates. | 🟡 MEDIUM (PUBLISHED-orphan gap, RISK-015) |
| `nap.case` | COMP-23 CaseManagementService, COMP-24 CaseActionService, COMP-27 CaseSearchService (lock-column UPDATE only) | Same shared-write risk as wdp.CASE, on the NAP schema. NAP/WDP lock-timestamp column-name drift (`x_locked_at` vs `z_locked_at`). | Same mitigation pattern as wdp.CASE. | 🟠 MEDIUM-HIGH |
| `nap.action` | COMP-23 CaseManagementService, COMP-24 CaseActionService | Same shared-write risk as wdp.ACTION, on the NAP schema. **NAP vs US functional asymmetry on COMP-24 EP 5** — NAP silently ignores `chbkOutbox` field on all branches. | Same pattern. | 🔴 HIGH — same RBAC absence |
| `nap.NOTES` | COMP-25 NotesService (primary), COMP-23 CaseManagementService (NAP create path), COMP-24 CaseActionService (conditional) | Multiple services write NAP notes. Dual-writer fact already documented; consider promoting to a fuller shared-table treatment at next reconciliation. | Per-component scoping of write conditions. | 🟡 MEDIUM |
| `NAP.DISPUTE_EVENT_CONSUMER_ERROR` | COMP-05 NAPDisputeEventProcessor (primary; owns DDL pattern), COMP-23 CaseManagementService (NAP create path blind-merge), COMP-24 CaseActionService (insert-path conditional, outbox-style UPDATE), **COMP-39 NAPOutcomeProcessor** (writes outbound-delivery error rows with C_ACQ_PLATFORM=`NAP`, C_EVENT_TYPE in {OUT_SRV118, OUT_SRV117}) | **(1) COMP-23 blind-merge** can produce sparse rows in a table it does not own — cross-component write into a COMP-05-owned table with no owner check. **(2) Cross-component reprocessing hazard:** COMP-39 prior-error scan filters on `caseNumber` + `sourceSystemName="NAP"` + status IN (FAILED1, FAILED2) **with no `C_EVENT_TYPE` filter**. Error records written by COMP-05, COMP-23, and COMP-24 against the same case may be scooped up and reprocessed through COMP-39's SRV118/SRV117 outbound pipeline regardless of their original origin. **COMP-05 has the same gap** — its prior-error scan filters on `arn` OR `networkCaseId` + `sourceSystemName='NAP'` only — no `C_EVENT_TYPE` filter; rows written by COMP-23/24/39 against the same case may be picked up and reprocessed through COMP-05's SRV116/WIN_LOSS/SRV118 pipeline. Both consumers share the same defect class — uniform architectural decision required (candidate ADR for shared-error-table consumption rules). **(3) PCI-DSS storage finding:** CVV is persisted in C_CVV column AND in the C_KAFKA_EVENT JSON blob. **(4) Ownership / write-contract clarification still required across COMP-05, COMP-23, COMP-24, COMP-39.** | None — write contract undocumented across the four components. | 🔴 HIGH — cross-component ownership not established + cross-component reprocessing hazard + PCI-DSS storage finding |
| `nap.nap_parent_entity` | COMP-02 UAMS (createEntity, updateEntity, deleteParentEntity) | DEC-021 wrong-TM on three methods — uses `wdpTransactionManager` instead of `napTransactionManager`. Rollback on failure may not cover nap schema writes. | None — confirmed bug | 🔴 HIGH |
| `nap.nap_child_entity` | COMP-02 UAMS (saveChildWithMerchant, createEntity, updateEntity, deleteChildEntity, deleteParentEntity) | DEC-021 wrong-TM on **5 of 7** known wrong-TM methods touching this table. Same root cause as `nap.nap_parent_entity`. | None — confirmed bug | 🔴 HIGH |
| `nap.nap_merchant` | COMP-02 UAMS (saveChildWithMerchant, deleteMerchantByParent, deleteChildEntity, deleteParentEntity) | DEC-021 wrong-TM on four methods. Same root cause. | None — confirmed bug | 🔴 HIGH |
| `nap.nap_entity_rel` | COMP-02 UAMS (saveChildWithMerchant, updateMerchantRelationships, deleteMerchantByParent, deleteChildEntity, deleteParentEntity) | DEC-021 wrong-TM on five methods. Same root cause. | None — confirmed bug | 🔴 HIGH |
| `nap.queues` / `nap.queue_criterion` / `nap.user_queue` | COMP-30 UserQueueSkillService | DEC-021 wrong-TM. Service-level `@Transactional` on `createQueue` / `updateQueue` binds to `@Primary usTransactionManager` — **UK writes NOT covered by the outer TX** (per-repository implicit TX only). Cross-repo other-writer sweep still pending. | None — confirmed bug | 🔴 HIGH |
| `nap.users` | COMP-02 UAMS (primary), COMP-30 UserQueueSkillService (POST /user side-effect — no `@Transactional`; auto-enrol runs in a separate implicit transaction; failure leaves orphaned user row) | Two writers with no coordination. Auto-enrol non-atomic; orphan-row failure mode. | None — confirmed | 🟡 MEDIUM |

---

## 5. Data Retention and Compliance Notes

| Data type | Retention requirement | Enforced by | Reference |
|-----------|----------------------|-------------|-----------|
| Dispute case audit trail | 7 years — immutable | Core WDP platform | PCI-DSS compliance |
| PAN data | Never stored in plaintext — ⚠️ DEC-004 / DEC-019 violations confirmed in: (a) COMP-23 standard case create on `wdp.CASE.I_ACCT_CDH` and `nap.case.I_ACCT_CDH`; (b) COMP-43 on `BC.TBC_DM_CASE.I_ACCT_CDH` (DB2 — clear PAN written from decrypted HPAN at Step 6 PAN-gate). PAN-last-4 column `I_ACCT_CDH_LST` is also written. **Architect decision required** (OQ-COMP-43-6) — document approved exception in WDP-DECISIONS.md, or remediate. | COMP-35 EncryptionService (enrichment path); gap on standard create path | DEC-004, DEC-007, DEC-019 |
| **CVV data** | Never stored — ⚠️ **PCI-DSS storage violation confirmed in `NAP.DISPUTE_EVENT_CONSUMER_ERROR`** on the NAP schema. CVV is persisted in two places per error row: (a) `C_CVV` column directly mapped on the entity; (b) raw `NotificationEvent` JSON serialised into `C_KAFKA_EVENT` which also contains the `cvv` field. Cross-link: COMP-04 NAPDisputeEventService logs the CVV; COMP-05 NAPDisputeEventProcessor persists it. **Architect decision required** — remediate at COMP-04 (suppress logging) and COMP-05 (redact before persistence) or document approved exception. | COMP-05 NAPDisputeEventProcessor (writer); COMP-04 NAPDisputeEventService (logger) | DEC-019 (storage); candidate ADR for redaction at error-table boundary |
| HPAN (hashed PAN) | Retained with case data | Core WDP platform | DEC-007 |
| EPAN (encrypted PAN) | Retained in EPAN→HPAN→ciphertext mapping table `wdp.hash_key_store`. KMS-wrapped DEKs in `wdp.data_enc_key`. Ciphertext never leaves COMP-35. | COMP-35 EncryptionService | DEC-007 |
| Evidence documents | ⚠️ Retention policy to be confirmed (S3 lifecycle / Terraform / CloudFormation review required) | COMP-37 DocumentManagementService (S3) | TBC |
| API audit logs | ⚠️ Retention policy to be confirmed | COMP-38 APILogService | TBC |
| Questionnaire data | ⚠️ Retention policy to be confirmed | COMP-26 QuestionnaireService | TBC |
| Outbox rows (chbk_outbox_row) | SUCCESS rows archived by COMP-12 Scheduler2 after 30 days. Archive table (chbk_outbox_row_archive) has no confirmed purge policy. ⚠️ Whether SKIPPED rows are archived is not currently confirmed — affects COMP-08 SKIPPED-marker accumulation boundedness. | COMP-12 InboundDisputeEventScheduler | Operational |
| Spring Batch metadata | ⚠️ Retention policy to be confirmed | Per batch component | TBC |
| `wdp.outgoing_event_outbox` PUBLISHED-status orphans | No automatic re-drive — manual runbook required for COMP-18, COMP-41, COMP-42, COMP-43 PUBLISHED-orphan paths. **Runbook owner not identified across audits.** Additionally, COMP-51 EXPIRY_BATCH rows are written terminally as ERROR with no consumer — separate architect decision. | Manual ops | RISK-015 (NFRS) |
| `NAP.DISPUTE_EVENT_CONSUMER_ERROR` reprocessing | No automatic owner check — both COMP-05 and COMP-39 read this table back via prior-error scans **without `C_EVENT_TYPE` filtering**; rows written by COMP-23 / COMP-24 / cross-component peers may be reprocessed through unintended pipelines. Architect decision pending. | Architect / candidate ADR | candidate ADR for shared-error-table consumption rules |

---

